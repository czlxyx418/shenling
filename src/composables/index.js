// import { onMounted, onUnmounted } from 'vue'

// const useResizeChart = (myChart) => {
//   // 监听窗口变化，重置 echarts
//   const echartsResize = () => {
//     myChart.resize()
//   }

//   // 监听窗口变化，重置 echarts
//   onMounted(() => {
//     window.addEventListener('resize', echartsResize)
//   })

//   // 销毁全局监听事件
//   onUnmounted(() => {
//     window.removeEventListener('resize', echartsResize)
//   })
// }

// export { useResizeChart }
